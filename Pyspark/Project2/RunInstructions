"""
This application can be run in Local Mode or with YARN
 Settings for YARN: (If Hadoop and Spark using YARN is already setup)
 --If using Pycharm set run > Edit Configurations in windows
   HADOOP_HOME=<Hadooppath>
   HADOOP_COMMON_HOME=<Hadooppath>
   HADOOP_CONF_DIR=<Hadooppath>/etc/hadoop
   YARN_CONF_DIR=<Sparkpath>/conf/yarn-conf

 --Start Hadoop
 --Run application from your IDE
"""
"""
To run from command line using spark-submit
--Open terminal
--Run application local mode
spark-submit --master local <path>TestApp1.py

--Run application on YARN (default is YARN)
spark-submit <path>TestApp1.py
"""
